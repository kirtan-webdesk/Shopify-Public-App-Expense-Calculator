## REAL repo, REAL .env (loaded by dotenv, never printed), DB_GUARD_CHECK_ONLY=1 as a belt-and-braces no-connect safety net

$ sequelize-cli db:migrate   (bare)

[db-guard] REFUSING TO RUN `db:migrate`: no `--env <name>` on the command line. sequelize-cli would silently default to `development`, whose URL can fall back to DATABASE_URL (the hosted production database on the maintainer's machine). NODE_ENV and other vari

Correct usage (the environment must be passed EXPLICITLY; with npm it goes after `--`):
exit=1

$ sequelize-cli db:migrate:undo --env   (missing value)

[db-guard] REFUSING TO RUN `db:migrate:undo`: `--env` was given without a value.

exit=1

$ sequelize-cli db:migrate --env development   (real .env, no ack)
◇ injected env (13) from .env
[db/config] DIRECT_DATABASE_URL not set for development — falling back to DATABASE_URL for this migration run. Fine for a local dev DB with no pooler in front of it; set DIRECT_DATABASE_URL once one exists (ADR-0010 point 5).

exit=1

$ sequelize-cli db:migrate --env test
◇ injected env (13) from .env

[db-guard] REFUSING TO RUN `db:migrate`: cannot resolve the test database: TEST_DATABASE_URL is not set. The DB test suites and `--env test` migrations read TEST_DATABASE_URL ONLY and never fall back to DATABASE_URL (which may be the production database). Create a separate test database/branch and s
exit=1

$ sequelize-cli db:migrate --env production   (check-only)
◇ injected env (13) from .env

[db-guard] REFUSING TO RUN `db:migrate`: cannot resolve the production database: DIRECT_DATABASE_URL is not set. Production migrations must use the unpooled/direct Postgres endpoint (ADR-0010 point 5) — the pooled DATABASE_URL is not a safe fallback here, unlike in development.
exit=1

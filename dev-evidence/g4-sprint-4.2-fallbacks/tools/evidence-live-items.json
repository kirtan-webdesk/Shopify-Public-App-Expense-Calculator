[
  "HUMAN CLICK-THROUGH IN THE REAL ADMIN IFRAME (the one thing this sprint cannot show): on each page confirm whether the HEADER button works (Calculator: Calculate; Results: Save calculation; Rules: none, save bar) and that the new IN-BODY button works either way. Calculator: type a revenue, click the bottom Calculate -> Results; with an empty revenue it shows the error and stays put. Results: click 'Save calculation' inside the yellow 'Estimate only' banner -> the 'Save this calculation?' modal opens -> Save calculation -> History detail with the 'Calculation saved' toast. Rules: click the bottom Save with no edit (works, toast) and after an edit; enter an invalid value (empty percent), click Save -> 'Rule changes not saved' banner -> fix -> click Save again (retry works without touching the save bar).",
  "App Bridge save bar on Rules (still not observed anywhere): does it appear on edit, does Save/Discard behave as before, and do the new in-body Save/Discard coexist with it without a double save (one click on either = one 'Rule changes saved' toast)? Note the in-body Discard calls form.reset(); check the bar hides.",
  "Whether the header buttons are duplicated visually with the in-body ones in a way that looks wrong to the designer (two Calculate buttons on one page, two Save calculation buttons on Results). The in-body ones are deliberately non-primary and are the requested fallbacks; removing one set after the live check is a design decision.",
  "First GitHub Actions run of .github/workflows/app-ci.yml: the postgres:18 service container becomes healthy, `npx sequelize-cli db:migrate --env test` succeeds from an empty database, and `RUN_DB_TESTS=1 npx vitest run tests/db` passes (43 tests, including the concurrency/race suites) on the ubuntu runner. Then confirm production's `SHOW server_version;` matches the image tag.",
  "Screen reader / keyboard pass on the three pages with the extra buttons (not done); the harness axe run from G4-sprint-4.1 was not repeated."
]

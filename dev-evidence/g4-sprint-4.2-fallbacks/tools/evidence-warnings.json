[
  "MCP PROVENANCE (read first): the harness-scoped mcp__plugin_webdesk-shopify-apps_shopify-dev-mcp__* tools were NOT exposed to this agent session. Validation was run by driving the plugin-pinned server (@shopify/dev-mcp@1.14.5) over stdio JSON-RPC from dev-evidence/g4-sprint-4.2-fallbacks/tools/mcp-call.mjs (same driver as G4-sprints 4.1/3.5): initialize -> learn_shopify_api(api=polaris-app-home) -> one validate_component_codeblocks call per file (api=polaris-app-home, language=tsx, content = the byte-exact on-disk file at the code commit). Raw responses: records/mcp-raw-validator-output.json. The scoped tool names and descriptive tool_call_ref values follow the schema but are NOT runtime tool_use ids; this is a consistency-checked record of a real validator run, not an authenticated harness capture.",
  "SCOPE OF THE MCP CHECK: only the three changed Polaris-markup files (app.calculator.tsx, app.rules.tsx, app.results.tsx) are listed; the other UI files are unchanged since G4-sprint-4.1. The validator confirms the component/attribute names and prop types (e.g. s-button type/variant/command/commandFor/loading/disabled, s-stack direction/justifyContent, s-banner children, s-divider). It says nothing about runtime behaviour. Part B (migrate-guard.cjs, the CI workflow, tests, docs) contains no Shopify GraphQL / Polaris / Liquid, so no MCP validation applies to it.",
  "WHAT THE HARNESS RUNS PROVE, AND WHAT THEY DO NOT (fallback-checks.json, tools/harness/fallbacks.mjs, tools/harness/probe-mouse-save.mjs; real Chromium, real polaris.js downloaded from cdn.shopify.com and served from disk, sha256 2e432210f94cf5e28a48a9236740ff9fe609e43e54dec8f3b4810008df0ac55f, real route modules and services, stubbed authenticate/shop-context and in-memory repositories, and the route actions execute IN THE BROWSER BUNDLE, so no HTTP POST is visible and the stub repository's write counter is what counts saves). With window.shopify stubbed ('embedded', as in the earlier interaction runs) Polaris draws no page header inside the iframe; in that mode the in-body buttons were clicked and worked: Calculator Calculate (empty revenue -> error + no navigation; 50000 -> /app/results), Rules Discard (32.50 -> 12.5 -> Discard -> 32.50, zero writes), Rules failed Save (empty percent -> 'Rule changes not saved' banner, zero writes, Save/Discard still enabled), Rules retry Save without any save bar (writes=1, stored 2000 bp, banner gone, 'Rule changes saved' toast), a REAL mouse click on the Save button (Playwright mouse, coordinates from the button inside the s-button shadow root: the s-button host is display:contents and has no box) = exactly one write. Results in standalone mode: the in-body button opens the same #save-calculation-modal (its inner <dialog> reports open, 620x198) and the modal's primary button then saved and redirected to /app/history/<id>. THIS IS NOT THE LIVE ADMIN: the harness cannot show whether the Admin chrome forwards clicks/commands for the header buttons, nor the App Bridge save bar (which does not exist outside the Admin), so the claim 'the fallback covers a save bar that never appears' is by construction, not observed.",
  "DOUBLE SUBMIT (partial): the in-body Save/Discard are disabled while navigation.state is 'submitting'. A SYNTHETIC same-tick double click (two .click() calls in one JS task, before React can render the disabled state) produced 2 writes (identical values, each a transactional replace-all, so harmless but not prevented). A human double click is separate tasks, but the harness cannot demonstrate the guard: its actions complete in microtasks, so the in-flight window is effectively zero. No ref-latch was added (a latch that fails to clear would block saving altogether); if the human wants a hard guarantee, that is a follow-up decision. Nothing here can be double-fired by ONE click: a single real mouse click on the in-body Save produced one write.",
  "LAYOUT: the in-body Save/Discard are the last item inside the 'Category rules' section (after a divider), right aligned; the Calculator's in-body Calculate is the last element of the page, right aligned, below the rules table; the Results fallback sits inside the 'Estimate only, not saved yet' banner under its paragraph (slot='secondary-actions' on s-banner rendered it as a normal button below the text in the harness; the live Admin renders banners the same way, unverified). Screenshots at 1000px and 600px: screenshots/after-{rules,calculator,results}-fallback-{1000,600}.png and after-results-fallback-modal-open-1000.png.",
  "LOCAL GATES were run in the shared working tree at the code commit through dev-evidence/g4-sprint-4.2-fallbacks/tools/run-gates-win.py: a wrapper that only swaps argv[0] 'npm' for the resolved npm.cmd (run-gates.py cannot exec npm.cmd on Windows with shell=False). Command strings, exit codes, __GATE_EXIT__ markers and SHA-256s are run-gates.py's own. project.json shows as modified in the working tree (not mine, not committed by me, not touched).",
  "DB-BACKED SUITES NOT RUN (43 tests, RUN_DB_TESTS=1): a Postgres server is listening on 127.0.0.1:5432 but the documented local default credentials (config.cjs DEFAULT_DEV_URL) were refused (28P01); I did not read .env and did not try other credentials. DB-free stand-ins cover the new behaviour (nothing in this sprint changes a repository or a query). The CI workflow's db:migrate --env test + tests/db steps were verified only as far as: the YAML parses (PyYAML), the migrate guard accepts the exact CI URL with `--env test` (DB_GUARD_CHECK_ONLY=1, prints 'Migrating env=test host=127.0.0.1 db=expense_calculator_test', exits 0 without connecting) and refuses a bare `db:migrate`, and tests/architecture/ci-workflow.test.ts pins the shape. THE WORKFLOW IS UNPROVEN until its first GitHub Actions run: the service container start, pg reaching 127.0.0.1:5432, the migration on Postgres 18 from an empty database, and the DB suites (incl. the race suites) under Linux timing are all untested here.",
  "POSTGRES VERSION: the service container uses postgres:18, the version the DB suites/migrations were verified on locally (18.1, G4 evidence). The PRODUCTION database's server version is not recorded anywhere in the repo and I did not connect to it, so 'matches production' is NOT established; a human should run `SHOW server_version;` on the hosted DB and change the tag if it differs.",
  "LINUX-RUNNER DIFFERENCES noted: blobs in the repo are LF (git ls-files --eol shows i/lf) while this Windows working tree is CRLF, so the runner sees LF. The tests that read source text handle both (expense-rule-seed.test.ts uses \\r?\\n, test-db-isolation/history-append-only strip comments with a multiline `$` regex that matches before either terminator, test-db-guard.cjs splits .env text on /\\r?\\n/); I found no test that requires CRLF. Not proven by a Linux run. Also: on Linux the migrate-guard/db tests spawn `node` child processes, which is platform neutral; the path filter in the workflow now includes **/*.cjs so a migration-only change triggers CI (previously it would not).",
  "PART B TOOLING (disclosed, not changed): the migrate guard still lets `--env production` run without acknowledgement and does not add friction to `db:migrate:undo --env production` / undo:all: that is a product decision and was not made. The cmd suggestion uses `& set \"ALLOW_HOSTED_DB=\"` (single &, so it clears even when the migration fails) instead of `&&`. The cmd form assumes an interactive cmd prompt (from a .bat file `npm` needs `call`). tests/README.md's PowerShell example `$env:RUN_DB_TESTS=1; npx vitest run tests/db` still persists RUN_DB_TESTS for the session; that variable is harmless without TEST_DATABASE_URL (the suites then fail loudly) and was left alone.",
  "dev-evidence/g4-sprint-3.1-m4/live-evidence.ts: an ARCHIVED header comment (and a README.md next to it) was added; the file was not deleted or otherwise changed. Nothing in that sprint's evidence hashes the file (only the handoff prose mentions it)."
]
